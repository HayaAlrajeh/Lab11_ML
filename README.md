# Credit Card Customer Segmentation using K-Means Clustering

This lab applies K-Means clustering to analyze credit card customer behavior.  
The dataset was cleaned, visualized, and scaled before applying clustering techniques.  
The Elbow Method and Silhouette Score were used to determine the optimal number of clusters and evaluate the model performance.
